pipeline {
    agent any
    tools {
        Maven "3.6.3"
    }
    stages {
        stage ('Compile Stage') {
            steps {
                    sh "mvn -v"
                    sh "mvn clean install"

            }
        }

    }

    post{
        always{
            cleanWs()
        }
    }
}