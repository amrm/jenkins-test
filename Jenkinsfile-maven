pipeline {
    agent any
    tools {
         maven "3.6.0"
      }
    stages {
        stage ('Compile Stage') {

            steps {
                    sh "mvn -v"
                    sh "mvn clean install"

            }
        }

    }

    post{
        always{
            cleanWs()
        }
    }
}