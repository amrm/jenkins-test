pipeline {
    agent any
    environment {
         PATH = "/opt/maven/bin"
      }
    stages {
        stage ('Compile Stage') {

            steps {
                    sh 'mvn clean'

            }
        }

    }
}